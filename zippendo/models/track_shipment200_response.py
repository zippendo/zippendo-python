# coding: utf-8

"""
    Zippendo Public API

    Public API documentation for Zippendo. Authenticate using your API token (Bearer token prefixed with zipp_).  **Brands (sub-accounts).** An organization can be split into brands, each keeping its own orders, shipments and configuration separate. There are two ways to scope requests to one brand, and NEITHER changes any request body:  1. **Bind the token.** Create an API token with a `brandId` and every request it makes is confined    to that brand — reads filtered, writes stamped. This is the recommended way to give a single    brand's team its own credential. 2. **Send the `X-Zippendo-Brand` header.** An organization-wide token can scope an individual    request by sending the brand's id or slug in this header. Most SDKs let you set it once on the    client so every call inherits it.  A brand-bound token that receives an `X-Zippendo-Brand` header naming a different brand is rejected with `403 BRAND_ACCESS_DENIED` — the binding is never widened. Omit both and requests cover the whole organization, which is the behaviour of every existing token.  Records that belong to no brand carry `brandId: null`. Configuration (carriers, shipping rules, addresses) with a null brand is organization-wide and remains visible inside every brand; orders and shipments with a null brand are only visible organization-wide.  List endpoints additionally take a `?brandScope=own|shared|both` parameter to narrow further within whichever brand context already applies. `own` returns only rows assigned to that brand, and requires a brand context — a brand-bound token, a resolved brand session, or the `X-Zippendo-Brand` header above — otherwise `400`. `shared` returns only the organization-wide rows (equivalent to filtering `brandId=none`). The default, `both`, keeps the existing behaviour: a brand context sees its own rows plus the organization-wide ones. Set `X-Zippendo-Brand-Scope` as a client default to apply the same choice to every request instead of repeating the query parameter on each call — an explicit `brandScope` query parameter always wins over the header, and a blank header value is ignored.  Brands themselves are managed under the **Brands** tag. Retiring a brand is done with `POST /orgs/{orgId}/brands/{brandId}/archive` — permanent deletion is a dashboard-only action, since it is refused while any order, shipment, member or token still references the brand. Brands require a plan that includes them; creating one beyond your plan's limit returns `403`.

    The version of the OpenAPI document: 1.0.0
    Contact: support@zippendo.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from zippendo.models.track_shipment200_response_events_inner import TrackShipment200ResponseEventsInner
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TrackShipment200Response(BaseModel):
    """
    TrackShipment200Response
    """ # noqa: E501
    tracking_number: Optional[StrictStr] = Field(description="Carrier tracking number.", alias="trackingNumber", json_schema_extra={"examples": ["00370724710000012345"]})
    tracking_url: Optional[StrictStr] = Field(description="Public carrier tracking URL.", alias="trackingUrl", json_schema_extra={"examples": ["https://tracking.postnord.com/00370724710000012345"]})
    current_status: Optional[StrictStr] = Field(description="Latest normalized tracking status.", alias="currentStatus", json_schema_extra={"examples": ["delivered"]})
    estimated_delivery: Optional[StrictStr] = Field(default=None, description="Estimated delivery timestamp.", alias="estimatedDelivery", json_schema_extra={"examples": ["2026-06-23T12:00:00.000Z"]})
    events: List[TrackShipment200ResponseEventsInner] = Field(description="Tracking events ordered from newest to oldest.")
    __properties: ClassVar[List[str]] = ["trackingNumber", "trackingUrl", "currentStatus", "estimatedDelivery", "events"]

    @field_validator('current_status')
    def current_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['created', 'awaiting_pickup', 'picked_up', 'in_transit', 'out_for_delivery', 'ready_for_pickup', 'delivered', 'returned', 'exception']):
            raise ValueError("must be one of enum values ('created', 'awaiting_pickup', 'picked_up', 'in_transit', 'out_for_delivery', 'ready_for_pickup', 'delivered', 'returned', 'exception')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrackShipment200Response from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in events (list)
        _items = []
        if self.events:
            for _item_events in self.events:
                if _item_events:
                    _items.append(_item_events.to_dict())
            _dict['events'] = _items
        # set to None if tracking_number (nullable) is None
        # and model_fields_set contains the field
        if self.tracking_number is None and "tracking_number" in self.model_fields_set:
            _dict['trackingNumber'] = None

        # set to None if tracking_url (nullable) is None
        # and model_fields_set contains the field
        if self.tracking_url is None and "tracking_url" in self.model_fields_set:
            _dict['trackingUrl'] = None

        # set to None if current_status (nullable) is None
        # and model_fields_set contains the field
        if self.current_status is None and "current_status" in self.model_fields_set:
            _dict['currentStatus'] = None

        # set to None if estimated_delivery (nullable) is None
        # and model_fields_set contains the field
        if self.estimated_delivery is None and "estimated_delivery" in self.model_fields_set:
            _dict['estimatedDelivery'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrackShipment200Response from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "trackingNumber": obj.get("trackingNumber"),
            "trackingUrl": obj.get("trackingUrl"),
            "currentStatus": obj.get("currentStatus"),
            "estimatedDelivery": obj.get("estimatedDelivery"),
            "events": [TrackShipment200ResponseEventsInner.from_dict(_item) for _item in obj["events"]] if obj.get("events") is not None else None
        })
        return _obj


